# Mirror from github.com/pdurlej/mirror-mirror

This repo is a private Forgejo mirror of the public GitHub repo.

- **Upstream**: https://github.com/pdurlej/mirror-mirror
- **Initial push**: 2026-05-17 by claude (cousin ID 3)
- **Attribution note**: The first force-push of `--master` and tags accidentally resolved
  the keychain PAT as `codex` (Multi-PAT keychain drift — see
  agent-souls/practices/credential-management.md). This marker commit is
  authored as `claude` to establish correct identity.

Commits in this mirror retain their upstream GitHub authorship (Piotr Durlej via
squash merge). Only this file and the bootstrap `.gitkeep` are claude-authored.
